<table>
  <tr>
    <td>name</td>
    <td>age</td>
  
  </tr>
  <%= @person.each do |person| %>
  <tr>
  <td><%= person.name %></td>
  <td><%= person.age %></td>
  <td><%= link_to 'show',person_path(person), :method => :get%></td>
  </tr>
  <% end%>
</table>