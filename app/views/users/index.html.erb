<!DOCTYPE html>
<html>
<head>
<style>
table {
  font-family: arial, sans-serif;
  border-collapse: collapse;
  width: 100%;
}

td, th {
  border: 1px solid #dddddd;
  text-align: left;
  padding: 8px;
}

tr:nth-child(even) {
  background-color: #dddddd;
}
</style>
</head>
<body>

<h2>HTML Table</h2>

<table>
  <tr>
    <th>ID</th>
    <th>First Name</th>
    <th>Last Name</th>
    <th>Age</th>
    <th>Joined Date</th>
    <th>Action</th>

  </tr>
  <% @users.each do |user| %>
  <tr>
    <td><%=user.id%></td>
    <td><%=user.first_name%></td>
    <td><%=user.last_name%></td>
    <td><%=user.age%></td>
    <td><%=user.created_at.strftime("%d/%m/%Y")%></td>
    <td><%= link_to 'show',users_path(user), :method => :get%></td>
  </tr>
  <% end%>
</table>

</body>
</html>

